#include <bits/stdc++.h>
using namespace std;
int main(){
	int n,k,m,p,l;
	cin>>n>>k>>m;
	
	int s[m];
	int count=0;
	map<int, int> hash;
	vector<vector<int> > v;
	for (int i = 0; i < k; ++i)
	{
		
		cin>>p;
		cin>>l;
		v[p]=l;
		
	}
	
	for (int i = 0; i < m; ++i)
	{
		cin>>s[i];
	}
		
}