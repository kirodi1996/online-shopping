#include <bits/stdc++.h>
int main(){
	vector <int> A;
	int n,s;
	cin>>n;
	for (int i = 0; i < n; ++i)
	{
		cin>>s;
		A.push_back(s);
	}
	
	int ones=0, two=0,three=0;
	for(int i=0;i<A.size();i++){
		while(A[i]!=0){
			
		}
	}
}